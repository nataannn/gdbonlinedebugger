#include <stdio.h>
#include <math.h>

float main ()
{
    float catA, catB, hip, hip1;
    
    printf("Informe o primeiro cateto: ");
    scanf("%f", &catA);
    
    printf("Informe o segundo cateto: ");
    scanf("%f", &catB);
    
    hip= (catA * catA) + (catB * catB);
    hip1=
    printf("A hipotenusa destes dois catetos é: %f", hip1);

    return 0;
}
